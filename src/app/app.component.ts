import { Component } from '@angular/core';
@Component({
  selector: 'app-root',
  template: `              
			    <hero-app> </hero-app> 			   
          <list-app> </list-app>
          `
})
export class AppComponent {
}
